@model TripsWithPagingViewModel

@{
    Layout = null;
}

<div class="col-lg-8 col-md-8 col-sm-12 col-xs-12" id="trips">
    <div class="trips-container text-center box-shadow">
        <div class="head box-shadow">
            <span>@this.Model.Heading</span>
        </div>

        <div class="main">
            @foreach (var trip in this.Model.Trips)
            {
                Html.RenderPartial("_TripItemPartial", trip);
            }

            <div class="pager text-center">
                @if (this.Model.Page == 1)
                {
                    <a id="prev-page" class="arrow arrow-left disabled"></a>
                }
                else
                {
                    <a id="prev-page" class="arrow arrow-left" href="@this.Model.PageHyperLink@(this.Model.Page - 1)"></a>
                }

                @if ((this.Model.Page * this.Model.Take) >= this.Model.Total)
                {
                    <a id="next-page" class="arrow arrow-right disabled"></a>
                }
                else
                {
                    <a id="next-page" class="arrow arrow-right" href="@this.Model.PageHyperLink@(this.Model.Page + 1)"></a>
                }
            </div>
        </div>
    </div>
</div>

