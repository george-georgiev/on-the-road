namespace OnTheRoad.Mvp.Enums
{
    public enum LoginStatus
    {
        Success = 0,
        LockedOut = 1,
        // TODO: Remove if not needed
        //RequiresVerification = 2,
        Failure = 2
    }
}